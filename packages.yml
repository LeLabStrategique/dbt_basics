packages:
  - package: fivetran/linkedin_pages
    # Utiliser une plage de versions pour éviter les ruptures majeures non testées
    version: [">=1.0.0", "<1.1.0"] 
  
  # Package essentiel pour les macros de modélisation (ex: clé synthétique)
  - package: dbt-labs/dbt_utils
    version: [">=1.1.1", "<2.0.0"]